import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:studysmma/app/modules/saved/saved_page.dart';

main() {
  testWidgets('SavedPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SavedPage(title: 'Saved')));
    //  final titleFinder = find.text('Saved');
    //  expect(titleFinder, findsOneWidget);
  });
}

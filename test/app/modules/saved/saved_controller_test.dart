import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:studysmma/app/modules/saved/saved_controller.dart';
import 'package:studysmma/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SavedController saved;
  //
  setUp(() {
    //     saved = AppModule.to.get<SavedController>();
  });

  group('SavedController Test', () {
    //   test("First Test", () {
    //     expect(saved, isInstanceOf<SavedController>());
    //   });

    //   test("Set Value", () {
    //     expect(saved.value, equals(0));
    //     saved.increment();
    //     expect(saved.value, equals(1));
    //   });
  });
}
